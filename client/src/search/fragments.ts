import { styled } from '@mui/material'

export const Page = styled('div')({
  padding: 20,
})

export const Header = styled('header')({
  marginBottom: 20,
  display: 'flex',
  gap: 20,
  alignItems: 'center',
})
